{
  "name": "reality-decision-prototype",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && node -e \"const fs=require('fs'); ['dist', 'app/dist'].forEach(d => fs.cpSync('frontend/dist', d, {recursive: true}));\""
  }
}
